---
layout: base.njk
title: 自我介绍
description: 滚动浏览我的自我介绍 — 关于我、在学的东西，还有我走过的路
keywords: 自我介绍, 个人介绍, 研究生, 学习
useAos: true
---

<section class="intro-hero">
  <div class="intro-hero-inner" data-aos="fade-up">
    <p class="intro-kicker">HELLO，我是</p>
    <h1 class="intro-name handwriting">{{ site.author }}</h1>
    <p class="intro-roles">爱做白日梦 <span class="dot">·</span> 经常摆烂 <span class="dot">·</span> 偶尔学习</p>
  </div>
  <div class="scroll-hint" data-aos="fade-up" data-aos-delay="500">
    <span class="scroll-hint-text">向下滚动，认识我</span>
    <div class="scroll-mouse"><div class="scroll-wheel"></div></div>
  </div>
</section>

<section class="intro-section">
  <div class="section-head" data-aos="fade-up">
    <span class="section-no">01</span>
    <h2 class="section-title">关于我</h2>
    <p class="section-sub">ABOUT ME</p>
  </div>

  <div class="about-row">
    <div class="about-avatar-wrap" data-aos="fade-right">
      <div class="about-avatar handwriting">白日<br>梦游</div>
      <div class="about-avatar-ring"></div>
    </div>
    <div class="about-text" data-aos="fade-left" data-aos-delay="150">
      <h3>你好，我是 {{ site.author }}，经常摆烂，偶尔学习。</h3>
      <p>没什么远大理想，就是想把正在学的东西弄明白一点，把踩过的坑记下来，留个记录。如果哪里做得不好，欢迎指出来，我会认真听、认真改的。</p>
      <p>这也算是个长期的练手作业。</p>
      <ul class="about-facts">
        <li><strong>坐标</strong><span>上海</span></li>
        <li><strong>方向</strong><span>后端/实施，还在摸索</span></li>
        <li><strong>信条</strong><span>慢慢来</span></li>
      </ul>
    </div>
  </div>
</section>

<section class="intro-section">
  <div class="section-head" data-aos="fade-up">
    <span class="section-no">02</span>
    <h2 class="section-title">我的技能</h2>
    <p class="section-sub">仅供参考</p>
  </div>

  <p class="skills-note" data-aos="fade-up">纯属个人自评，水平有限，仅供参考：</p>
  <div class="skill-tier" data-aos="fade-up">
    <h3>偶尔还用的</h3>
    <div class="skill-chips">
      <span class="skill-chip">Java</span>
      <span class="skill-chip">SQL</span>
    </div>
  </div>
  <div class="skill-tier" data-aos="fade-up" data-aos-delay="80">
    <h3>用过、但会忘的</h3>
    <div class="skill-chips">
      <span class="skill-chip">Python</span>
      <span class="skill-chip">JavaScript</span>
    </div>
  </div>
  <div class="skill-tier" data-aos="fade-up" data-aos-delay="160">
    <h3>只是了解过的</h3>
    <div class="skill-chips">
      <span class="skill-chip">C#</span>
      <span class="skill-chip">C/C++</span>
    </div>
  </div>
</section>

<section class="intro-section">
  <div class="section-head" data-aos="fade-up">
    <span class="section-no">03</span>
    <h2 class="section-title">走过的路</h2>
    <p class="section-sub">MY JOURNEY</p>
  </div>

  <div class="journey">
    <div class="journey-item" data-aos="fade-up">
      <div class="journey-date">2026.9 — 至今</div>
      <div class="journey-card">
        <h4>上海大学</h4>
        <p>希望这几年也会有收获。</p>
      </div>
    </div>
    <div class="journey-item" data-aos="fade-up">
      <div class="journey-date">2026.6</div>
      <div class="journey-card">
        <h4>上海电力大学</h4>
        <p>毕业了，这四年把我过爽了。</p>
      </div>
    </div>
    <div class="journey-item" data-aos="fade-up">
      <div class="journey-date">2026.4 — 2026.6</div>
      <div class="journey-card">
        <h4>上海万达信息科技有限公司</h4>
        <p>上海人社局的业务我没太看懂，不过早九晚五确实舒服。</p>
      </div>
    </div>
    <div class="journey-item" data-aos="fade-up">
      <div class="journey-date">2024.7 — 2024.9</div>
      <div class="journey-card">
        <h4>上海中科卫投信息科技有限公司</h4>
        <p>第一次接触工业级代码，领导和同事都挺好，就是任务有点多，有点重。</p>
      </div>
    </div>
    <div class="journey-item" data-aos="fade-up">
      <div class="journey-date">2022.9</div>
      <div class="journey-card">
        <h4>上海电力大学</h4>
        <p>写下人生第一行 Hello World。</p>
      </div>
    </div>
  </div>
</section>

<section class="intro-section">
  <div class="section-head" data-aos="fade-up">
    <span class="section-no">04</span>
    <h2 class="section-title">我的一点坚持</h2>
    <p class="section-sub">LITTLE HABITS</p>
  </div>

  <div class="values-grid">
    <div class="value-card" data-aos="flip-up" data-aos-delay="0">
      <span class="value-num">01</span>
      <h4>慢慢来</h4>
      <p>慢工出烂活，欲速则一坨</p>
    </div>
    <div class="value-card" data-aos="flip-up" data-aos-delay="120">
      <span class="value-num">02</span>
      <h4>多记录</h4>
      <p>没有好记性，那只能靠烂笔头</p>
    </div>
    <div class="value-card" data-aos="flip-up" data-aos-delay="240">
      <span class="value-num">03</span>
      <h4>多请教</h4>
      <p>不懂不硬扛，装懂慌得够呛</p>
    </div>
    <div class="value-card" data-aos="flip-up" data-aos-delay="360">
      <span class="value-num">04</span>
      <h4>多动手</h4>
      <p>眼睛说会了，手表示：你会个嘚呀</p>
    </div>
    <div class="value-card" data-aos="flip-up" data-aos-delay="480">
      <span class="value-num">05</span>
      <h4>少熬夜</h4>
      <p>通宵一时爽，垮掉悔断肠，身体比什么都重要</p>
    </div>
    <div class="value-card" data-aos="flip-up" data-aos-delay="600">
      <span class="value-num">06</span>
      <h4>按时吃饭</h4>
      <p>任务再繁重，干饭才是根本。</p>
    </div>
  </div>
</section>

<section class="intro-section intro-contact">
  <div class="contact-card" data-aos="zoom-in-up">
    <h2 class="handwriting">很高兴认识你</h2>
    <p>如果你想聊聊学习、代码，或者一起做点什么有意思的事，欢迎随时找到我。</p>
    <div class="contact-links">
      <button type="button" class="contact-btn" data-reveal="{{ site.qq }}">QQ</button>
      <button type="button" class="contact-btn contact-btn-warm" data-reveal="{{ site.email }}">邮箱</button>
    </div>
    <script>
      document.querySelectorAll('.contact-btn[data-reveal]').forEach(function (btn) {
        btn.addEventListener('click', function () {
          btn.textContent = btn.getAttribute('data-reveal');
        });
      });
    </script>
  </div>
</section>

<style>
/* 防止横向溢出（侧向动画会推宽页面） */
html, body { overflow-x: hidden; }

.intro-hero {
  min-height: calc(100vh - 88px);
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
  position: relative;
  padding: 4rem 0 2rem;
}

.intro-hero::before,
.intro-hero::after {
  content: '';
  position: absolute;
  border-radius: 50%;
  filter: blur(60px);
  z-index: 0;
  pointer-events: none;
}

.intro-hero::before {
  width: 360px;
  height: 360px;
  background: rgba(92, 141, 137, 0.16);
  top: 8%;
  left: 6%;
}

.intro-hero::after {
  width: 300px;
  height: 300px;
  background: rgba(200, 130, 110, 0.16);
  bottom: 10%;
  right: 8%;
}

.intro-hero-inner {
  position: relative;
  z-index: 1;
}

.intro-kicker {
  letter-spacing: 0.35em;
  font-size: 0.8rem;
  color: var(--accent-warm);
  margin-bottom: 1rem;
}

.intro-name {
  font-size: 4.5rem;
  color: var(--text-primary);
  margin-bottom: 1.2rem;
}

.intro-roles {
  font-size: 0.95rem;
  color: var(--text-muted);
  letter-spacing: 0.08em;
}

.intro-roles .dot {
  margin: 0 0.5rem;
}

.scroll-hint {
  position: absolute;
  bottom: 1.5rem;
  left: 45%;
  transform: translateX(-50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.6rem;
  z-index: 1;
}

.scroll-hint-text {
  font-size: 0.78rem;
  color: var(--text-muted);
  letter-spacing: 0.2em;
}

.scroll-mouse {
  width: 22px;
  height: 36px;
  border: 2px solid var(--text-muted);
  border-radius: 12px;
  display: flex;
  justify-content: center;
  padding-top: 6px;
}

.scroll-wheel {
  width: 3px;
  height: 7px;
  border-radius: 2px;
  background: var(--accent);
  animation: wheelDrop 1.6s ease-in-out infinite;
}

@keyframes wheelDrop {
  0% { opacity: 1; transform: translateY(0); }
  70% { opacity: 0; transform: translateY(10px); }
  100% { opacity: 0; transform: translateY(10px); }
}

.intro-section {
  padding: 4.5rem 0;
  border-top: 1px solid var(--border-color);
}

.section-head {
  text-align: center;
  margin-bottom: 3rem;
}

.section-no {
  display: inline-block;
  font-size: 0.78rem;
  letter-spacing: 0.25em;
  color: var(--accent-warm);
  border: 1px solid var(--border-color);
  border-radius: 999px;
  padding: 0.2rem 0.9rem;
  margin-bottom: 0.9rem;
  background: var(--bg-card);
}

.section-title {
  font-size: 2.2rem;
  margin-bottom: 0.2rem;
}

.section-sub {
  font-size: 0.82rem;
  letter-spacing: 0.3em;
  color: var(--text-muted);
}

/* 关于我 */
.about-row {
  display: grid;
  grid-template-columns: 280px 1fr;
  gap: 3rem;
  align-items: center;
  max-width: 900px;
  margin: 0 auto;
}

.about-avatar-wrap {
  position: relative;
  display: flex;
  justify-content: center;
}

.about-avatar {
  width: 220px;
  height: 220px;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--accent), var(--accent-warm));
  color: #fff;
  font-size: 2rem;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  line-height: 1.4;
  position: relative;
  z-index: 1;
  box-shadow: var(--shadow-lg);
}

.about-avatar-ring {
  position: absolute;
  inset: -14px;
  border-radius: 50%;
  border: 1px dashed var(--accent-light);
  animation: spin 30s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.about-text h3 {
  font-size: 1.6rem;
  margin-bottom: 1rem;
}

.about-text p {
  color: var(--text-secondary);
  line-height: 1.9;
  margin-bottom: 0.9rem;
}

.about-facts {
  list-style: none;
  padding: 0;
  margin-top: 1.4rem;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0.5rem 1.5rem;
}

.about-facts li {
  display: flex;
  gap: 0.5rem;
  font-size: 0.92rem;
  color: var(--text-secondary);
  border-bottom: 1px solid var(--divider);
  padding-bottom: 0.5rem;
}

.about-facts strong {
  color: var(--accent);
  font-weight: 600;
  min-width: 2.2em;
}

/* 技能 */
.skills-note {
  text-align: center;
  font-size: 0.9rem;
  color: var(--text-muted);
  margin-bottom: 1.6rem;
}

.skill-tier {
  max-width: 560px;
  margin: 0 auto 1.5rem;
  text-align: center;
}

.skill-tier h3 {
  font-size: 1rem;
  color: var(--accent);
  margin-bottom: 0.6rem;
}

.skill-chips {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 0.5rem;
}

.skill-chip {
  display: inline-block;
  padding: 0.3rem 0.9rem;
  font-size: 0.85rem;
  border-radius: 999px;
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  color: var(--text-secondary);
}

/* 走过的路 */
.journey {
  position: relative;
  max-width: 820px;
  margin: 0 auto;
  padding: 1rem 0;
}

.journey::before {
  content: '';
  position: absolute;
  left: 50%;
  top: 0;
  bottom: 0;
  width: 1px;
  background: var(--border-color);
  transform: translateX(-50%);
}

.journey-item {
  position: relative;
  margin-bottom: 2.5rem;
}

.journey-item:nth-child(odd) .journey-card {
  margin-right: 52%;
}

.journey-item:nth-child(even) .journey-card {
  margin-left: 52%;
}

.journey-item::after {
  content: '';
  position: absolute;
  left: 50%;
  top: 12px;
  width: 11px;
  height: 11px;
  border-radius: 50%;
  background: var(--accent);
  border: 2px solid var(--bg-primary);
  transform: translateX(-50%);
  z-index: 1;
}

.journey-card {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 8px;
  padding: 1.1rem 1.3rem;
}

.journey-date {
  font-size: 0.8rem;
  color: var(--accent-warm);
  font-weight: 600;
  margin-bottom: 0.35rem;
  letter-spacing: 0.04em;
}

.journey-card h4 {
  font-size: 1.05rem;
  margin-bottom: 0.3rem;
}

.journey-card p {
  font-size: 0.88rem;
  color: var(--text-secondary);
  line-height: 1.7;
}

/* 坚持 */
.values-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 1.25rem;
  max-width: 900px;
  margin: 0 auto;
}

.value-card {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 10px;
  padding: 1.6rem 1.4rem;
  text-align: center;
  transition: box-shadow 0.25s ease, transform 0.2s ease;
}

.value-card:hover {
  box-shadow: var(--shadow-md);
  transform: translateY(-3px);
}

.value-num {
  font-family: 'Kalam', cursive;
  font-size: 1.6rem;
  color: var(--accent-light);
  display: block;
  margin-bottom: 0.5rem;
}

.value-card h4 {
  font-size: 1.05rem;
  margin-bottom: 0.5rem;
  color: var(--accent);
}

.value-card p {
  font-size: 0.85rem;
  color: var(--text-secondary);
  line-height: 1.6;
}

/* 联系 */
.intro-contact {
  border-bottom: 1px solid var(--border-color);
}

.contact-card {
  max-width: 640px;
  margin: 0 auto;
  text-align: center;
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 14px;
  padding: 3rem 2.5rem;
  background-image: radial-gradient(circle at 15% 20%, rgba(92,141,137,0.1), transparent 60%),
    radial-gradient(circle at 85% 80%, rgba(200,130,110,0.12), transparent 60%);
}

.contact-card h2 {
  font-size: 2.2rem;
  margin-bottom: 0.8rem;
}

.contact-card p {
  color: var(--text-secondary);
  margin-bottom: 2rem;
}

.contact-links {
  display: flex;
  gap: 1rem;
  justify-content: center;
}

.contact-btn {
  display: inline-block;
  padding: 0.7rem 1.8rem;
  border: none;
  border-radius: 999px;
  font-family: inherit;
  font-size: 0.92rem;
  font-weight: 500;
  color: #fff;
  background: var(--accent);
  cursor: pointer;
  transition: background 0.2s ease, transform 0.2s ease;
}

.contact-btn:hover {
  background: var(--accent-hover);
  transform: translateY(-2px);
}

.contact-btn-warm {
  background: var(--accent-warm);
}

.contact-btn-warm:hover {
  background: var(--accent-warm-hover);
}

@media (max-width: 768px) {
  .intro-name {
    font-size: 3rem;
  }

  .about-row {
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  .about-facts {
    grid-template-columns: 1fr;
  }

  .journey::before {
    left: 16px;
  }

  .journey-item:nth-child(odd) .journey-card,
  .journey-item:nth-child(even) .journey-card {
    margin-left: 40px;
    margin-right: 0;
  }

  .journey-item::after {
    left: 16px;
  }

  .contact-card {
    padding: 2.2rem 1.5rem;
  }
}
</style>
