---
layout: base.njk
title: 自我介绍
description: 滚动浏览我的自我介绍 — 关于我、技能、成长轨迹与热爱
keywords: 自我介绍, 个人介绍, 简历, 技能, 经历
useAos: true
---

<section class="intro-hero">
  <div class="intro-hero-inner" data-aos="fade-up">
    <p class="intro-kicker">HELLO，我是</p>
    <h1 class="intro-name handwriting">{{ site.author }}</h1>
    <p class="intro-tagline">{{ site.motto }}</p>
    <p class="intro-roles">前端开发者 <span class="dot">·</span> 文字记录者 <span class="dot">·</span> 生活热爱者</p>
  </div>
  <div class="scroll-hint" data-aos="fade-up" data-aos-delay="500">
    <span class="scroll-hint-text">向下滚动，认识我</span>
    <div class="scroll-mouse"><div class="scroll-wheel"></div></div>
  </div>
</section>

<section class="intro-section">
  <div class="section-head" data-aos="fade-up">
    <span class="section-no">01</span>
    <h2 class="section-title">关于我</h2>
    <p class="section-sub">ABOUT ME</p>
  </div>

  <div class="about-row">
    <div class="about-avatar-wrap" data-aos="fade-right">
      <div class="about-avatar handwriting">白日<br>梦游</div>
      <div class="about-avatar-ring"></div>
    </div>
    <div class="about-text" data-aos="fade-left" data-aos-delay="150">
      <h3>你好，我是 {{ site.author }}，一个爱做梦的开发者。</h3>
      <p>我坚信代码不只是逻辑的堆叠，更是一种表达方式。在这个数字化的世界里，我试着用代码搭建有温度的产品，用文字记录有思考的生活。</p>
      <p>「{{ site.signature }}」这个名字，寄托了我对生活最朴素的期待 —— 把白日梦走成现实。白天认真写代码，夜晚在文字里漫游，这就是我最舒服的状态。</p>
      <ul class="about-facts">
        <li><strong>坐标</strong><span>中国 · 一个慢生活的小城</span></li>
        <li><strong>方向</strong><span>前端 / 全栈 / 内容创作</span></li>
        <li><strong>信条</strong><span>保持好奇，慢慢生长</span></li>
      </ul>
    </div>
  </div>
</section>

<section class="intro-section">
  <div class="section-head" data-aos="fade-up">
    <span class="section-no">02</span>
    <h2 class="section-title">我的技能</h2>
    <p class="section-sub">WHAT I DO</p>
  </div>

  <div class="skill-grid">
    <div class="skill-card" data-aos="zoom-in" data-aos-delay="0">
      <div class="skill-card-icon">⌘</div>
      <h4>前端开发</h4>
      <p>HTML / CSS / JavaScript，喜欢打磨细腻的交互与视觉</p>
    </div>
    <div class="skill-card" data-aos="zoom-in" data-aos-delay="100">
      <div class="skill-card-icon">⚙</div>
      <h4>框架与工具</h4>
      <p>Vue / React / Node.js，关注工程化与可维护性</p>
    </div>
    <div class="skill-card" data-aos="zoom-in" data-aos-delay="200">
      <div class="skill-card-icon">✒</div>
      <h4>设计与写作</h4>
      <p>对美学有执念，用文字记录思考与生活片段</p>
    </div>
  </div>

  <div class="skill-bars">
    <div class="skill-bar-row" data-aos="fade-up" data-aos-delay="0">
      <div class="skill-bar-head"><span class="skill-bar-name">JavaScript</span><span class="skill-bar-value">90%</span></div>
      <div class="skill-bar-track"><div class="skill-bar-fill" style="--w: 90%"></div></div>
    </div>
    <div class="skill-bar-row" data-aos="fade-up" data-aos-delay="80">
      <div class="skill-bar-head"><span class="skill-bar-name">HTML &amp; CSS</span><span class="skill-bar-value">88%</span></div>
      <div class="skill-bar-track"><div class="skill-bar-fill" style="--w: 88%"></div></div>
    </div>
    <div class="skill-bar-row" data-aos="fade-up" data-aos-delay="160">
      <div class="skill-bar-head"><span class="skill-bar-name">Vue / React</span><span class="skill-bar-value">82%</span></div>
      <div class="skill-bar-track"><div class="skill-bar-fill" style="--w: 82%"></div></div>
    </div>
    <div class="skill-bar-row" data-aos="fade-up" data-aos-delay="240">
      <div class="skill-bar-head"><span class="skill-bar-name">Node.js</span><span class="skill-bar-value">72%</span></div>
      <div class="skill-bar-track"><div class="skill-bar-fill" style="--w: 72%"></div></div>
    </div>
  </div>
</section>

<section class="intro-section">
  <div class="section-head" data-aos="fade-up">
    <span class="section-no">03</span>
    <h2 class="section-title">成长轨迹</h2>
    <p class="section-sub">MY JOURNEY</p>
  </div>

  <div class="journey">
    <div class="journey-item" data-aos="fade-right">
      <div class="journey-date">2024 — 至今</div>
      <div class="journey-card">
        <h4>独立开发者 / 内容创作者</h4>
        <p>搭建自己的数字花园，把「白日梦游记」经营成一个有温度的个人站点，写代码也写文字。</p>
      </div>
    </div>
    <div class="journey-item" data-aos="fade-left">
      <div class="journey-date">2021 — 2024</div>
      <div class="journey-card">
        <h4>全栈开发工程师</h4>
        <p>参与多个创新项目，从需求分析到产品上线全流程参与，学会了在约束中做取舍。</p>
      </div>
    </div>
    <div class="journey-item" data-aos="fade-right">
      <div class="journey-date">2019 — 2021</div>
      <div class="journey-card">
        <h4>前端开发工程师</h4>
        <p>专注用户界面与交互体验，开始相信好的技术应该让生活更轻盈。</p>
      </div>
    </div>
    <div class="journey-item" data-aos="fade-left">
      <div class="journey-date">2015 — 2019</div>
      <div class="journey-card">
        <h4>计算机科学学士</h4>
        <p>大学期间打下编程基础，也养成了阅读与记录的习惯 —— 这可能是我最重要的财富。</p>
      </div>
    </div>
  </div>
</section>

<section class="intro-section">
  <div class="section-head" data-aos="fade-up">
    <span class="section-no">04</span>
    <h2 class="section-title">价值理念</h2>
    <p class="section-sub">WHAT I BELIEVE</p>
  </div>

  <div class="values-grid">
    <div class="value-card" data-aos="flip-up" data-aos-delay="0">
      <span class="value-num">01</span>
      <h4>精益求精</h4>
      <p>对细节不妥协，但知道什么时候该停下来。</p>
    </div>
    <div class="value-card" data-aos="flip-up" data-aos-delay="120">
      <span class="value-num">02</span>
      <h4>持续学习</h4>
      <p>保持好奇心，在变化里寻找成长的缝隙。</p>
    </div>
    <div class="value-card" data-aos="flip-up" data-aos-delay="240">
      <span class="value-num">03</span>
      <h4>团队协作</h4>
      <p>相信一群人走得更远，沟通比聪明更重要。</p>
    </div>
    <div class="value-card" data-aos="flip-up" data-aos-delay="360">
      <span class="value-num">04</span>
      <h4>诗意生活</h4>
      <p>技术要有温度，生活需要仪式感。</p>
    </div>
  </div>
</section>

<section class="intro-section intro-contact">
  <div class="contact-card" data-aos="zoom-in-up">
    <h2 class="handwriting">很高兴认识你</h2>
    <p>如果你想聊聊技术、文字，或者一起做点什么有意思的事，欢迎随时找到我。</p>
    <div class="contact-links">
      <a href="{{ site.github }}" class="contact-btn" target="_blank" rel="noopener noreferrer">GitHub</a>
      <a href="{{ site.twitter }}" class="contact-btn contact-btn-warm" target="_blank" rel="noopener noreferrer">Twitter</a>
    </div>
  </div>
</section>

<style>
/* 防止横向溢出（侧向动画会推宽页面） */
html, body { overflow-x: hidden; }

.intro-hero {
  min-height: calc(100vh - 88px);
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
  position: relative;
  padding: 4rem 0 2rem;
}

.intro-hero::before,
.intro-hero::after {
  content: '';
  position: absolute;
  border-radius: 50%;
  filter: blur(60px);
  z-index: 0;
  pointer-events: none;
}

.intro-hero::before {
  width: 360px;
  height: 360px;
  background: rgba(92, 141, 137, 0.16);
  top: 8%;
  left: 6%;
}

.intro-hero::after {
  width: 300px;
  height: 300px;
  background: rgba(200, 130, 110, 0.16);
  bottom: 10%;
  right: 8%;
}

.intro-hero-inner {
  position: relative;
  z-index: 1;
}

.intro-kicker {
  letter-spacing: 0.35em;
  font-size: 0.8rem;
  color: var(--accent-warm);
  margin-bottom: 1rem;
}

.intro-name {
  font-size: 4.5rem;
  color: var(--text-primary);
  margin-bottom: 0.4rem;
}

.intro-tagline {
  font-size: 1.3rem;
  color: var(--text-secondary);
  margin-bottom: 1.2rem;
}

.intro-roles {
  font-size: 0.95rem;
  color: var(--text-muted);
  letter-spacing: 0.08em;
}

.intro-roles .dot {
  margin: 0 0.5rem;
}

.scroll-hint {
  position: absolute;
  bottom: 1.5rem;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.6rem;
  z-index: 1;
}

.scroll-hint-text {
  font-size: 0.78rem;
  color: var(--text-muted);
  letter-spacing: 0.2em;
}

.scroll-mouse {
  width: 22px;
  height: 36px;
  border: 2px solid var(--text-muted);
  border-radius: 12px;
  display: flex;
  justify-content: center;
  padding-top: 6px;
}

.scroll-wheel {
  width: 3px;
  height: 7px;
  border-radius: 2px;
  background: var(--accent);
  animation: wheelDrop 1.6s ease-in-out infinite;
}

@keyframes wheelDrop {
  0% { opacity: 1; transform: translateY(0); }
  70% { opacity: 0; transform: translateY(10px); }
  100% { opacity: 0; transform: translateY(10px); }
}

.intro-section {
  padding: 4.5rem 0;
  border-top: 1px solid var(--border-color);
}

.section-head {
  text-align: center;
  margin-bottom: 3rem;
}

.section-no {
  display: inline-block;
  font-size: 0.78rem;
  letter-spacing: 0.25em;
  color: var(--accent-warm);
  border: 1px solid var(--border-color);
  border-radius: 999px;
  padding: 0.2rem 0.9rem;
  margin-bottom: 0.9rem;
  background: var(--bg-card);
}

.section-title {
  font-size: 2.2rem;
  margin-bottom: 0.2rem;
}

.section-sub {
  font-size: 0.82rem;
  letter-spacing: 0.3em;
  color: var(--text-muted);
}

/* 关于我 */
.about-row {
  display: grid;
  grid-template-columns: 280px 1fr;
  gap: 3rem;
  align-items: center;
  max-width: 900px;
  margin: 0 auto;
}

.about-avatar-wrap {
  position: relative;
  display: flex;
  justify-content: center;
}

.about-avatar {
  width: 220px;
  height: 220px;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--accent), var(--accent-warm));
  color: #fff;
  font-size: 2rem;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  line-height: 1.4;
  position: relative;
  z-index: 1;
  box-shadow: var(--shadow-lg);
}

.about-avatar-ring {
  position: absolute;
  inset: -14px;
  border-radius: 50%;
  border: 1px dashed var(--accent-light);
  animation: spin 30s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.about-text h3 {
  font-size: 1.6rem;
  margin-bottom: 1rem;
}

.about-text p {
  color: var(--text-secondary);
  line-height: 1.9;
  margin-bottom: 0.9rem;
}

.about-facts {
  list-style: none;
  padding: 0;
  margin-top: 1.4rem;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0.5rem 1.5rem;
}

.about-facts li {
  display: flex;
  gap: 0.5rem;
  font-size: 0.92rem;
  color: var(--text-secondary);
  border-bottom: 1px solid var(--divider);
  padding-bottom: 0.5rem;
}

.about-facts strong {
  color: var(--accent);
  font-weight: 600;
  min-width: 2.2em;
}

/* 技能 */
.skill-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 1.25rem;
  margin-bottom: 2.5rem;
}

.skill-card {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 10px;
  padding: 1.8rem 1.5rem;
  text-align: center;
  transition: box-shadow 0.25s ease, transform 0.2s ease;
}

.skill-card:hover {
  box-shadow: var(--shadow-md);
  transform: translateY(-3px);
}

.skill-card-icon {
  width: 52px;
  height: 52px;
  margin: 0 auto 0.9rem;
  border-radius: 50%;
  background: var(--bg-secondary);
  border: 1px solid var(--border-color);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.3rem;
  color: var(--accent);
}

.skill-card h4 {
  font-size: 1.05rem;
  margin-bottom: 0.5rem;
}

.skill-card p {
  font-size: 0.85rem;
  color: var(--text-secondary);
  line-height: 1.6;
}

.skill-bars {
  max-width: 720px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: 1.1rem;
}

.skill-bar-head {
  display: flex;
  justify-content: space-between;
  font-size: 0.9rem;
  margin-bottom: 0.4rem;
}

.skill-bar-name {
  color: var(--text-primary);
  font-weight: 500;
}

.skill-bar-value {
  color: var(--accent-warm);
  font-weight: 600;
}

.skill-bar-track {
  height: 8px;
  border-radius: 999px;
  background: var(--bg-secondary);
  border: 1px solid var(--border-color);
  overflow: hidden;
}

.skill-bar-fill {
  width: 0;
  height: 100%;
  border-radius: inherit;
  background: linear-gradient(90deg, var(--accent), var(--accent-warm));
  transition: width 1.1s cubic-bezier(0.4, 0, 0.2, 1) 0.25s;
}

.skill-bar-row.aos-animate .skill-bar-fill {
  width: var(--w);
}

/* 成长轨迹 */
.journey {
  position: relative;
  max-width: 820px;
  margin: 0 auto;
  padding: 1rem 0;
}

.journey::before {
  content: '';
  position: absolute;
  left: 50%;
  top: 0;
  bottom: 0;
  width: 1px;
  background: var(--border-color);
  transform: translateX(-50%);
}

.journey-item {
  position: relative;
  margin-bottom: 2.5rem;
}

.journey-item:nth-child(odd) .journey-card {
  margin-right: 52%;
}

.journey-item:nth-child(even) .journey-card {
  margin-left: 52%;
}

.journey-item::after {
  content: '';
  position: absolute;
  left: 50%;
  top: 12px;
  width: 11px;
  height: 11px;
  border-radius: 50%;
  background: var(--accent);
  border: 2px solid var(--bg-primary);
  transform: translateX(-50%);
  z-index: 1;
}

.journey-card {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 8px;
  padding: 1.1rem 1.3rem;
}

.journey-date {
  font-size: 0.8rem;
  color: var(--accent-warm);
  font-weight: 600;
  margin-bottom: 0.35rem;
  letter-spacing: 0.04em;
}

.journey-card h4 {
  font-size: 1.05rem;
  margin-bottom: 0.3rem;
}

.journey-card p {
  font-size: 0.88rem;
  color: var(--text-secondary);
  line-height: 1.7;
}

/* 价值理念 */
.values-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 1.25rem;
  max-width: 900px;
  margin: 0 auto;
}

.value-card {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 10px;
  padding: 1.6rem 1.4rem;
  text-align: center;
  transition: box-shadow 0.25s ease, transform 0.2s ease;
}

.value-card:hover {
  box-shadow: var(--shadow-md);
  transform: translateY(-3px);
}

.value-num {
  font-family: 'Kalam', cursive;
  font-size: 1.6rem;
  color: var(--accent-light);
  display: block;
  margin-bottom: 0.5rem;
}

.value-card h4 {
  font-size: 1.05rem;
  margin-bottom: 0.5rem;
  color: var(--accent);
}

.value-card p {
  font-size: 0.85rem;
  color: var(--text-secondary);
  line-height: 1.6;
}

/* 联系 */
.intro-contact {
  border-bottom: 1px solid var(--border-color);
}

.contact-card {
  max-width: 640px;
  margin: 0 auto;
  text-align: center;
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 14px;
  padding: 3rem 2.5rem;
  background-image: radial-gradient(circle at 15% 20%, rgba(92,141,137,0.1), transparent 60%),
    radial-gradient(circle at 85% 80%, rgba(200,130,110,0.12), transparent 60%);
}

.contact-card h2 {
  font-size: 2.2rem;
  margin-bottom: 0.8rem;
}

.contact-card p {
  color: var(--text-secondary);
  margin-bottom: 2rem;
}

.contact-links {
  display: flex;
  gap: 1rem;
  justify-content: center;
}

.contact-btn {
  display: inline-block;
  padding: 0.7rem 1.8rem;
  border-radius: 999px;
  text-decoration: none;
  font-size: 0.92rem;
  font-weight: 500;
  color: #fff;
  background: var(--accent);
  transition: background 0.2s ease, transform 0.2s ease;
}

.contact-btn:hover {
  background: var(--accent-hover);
  transform: translateY(-2px);
}

.contact-btn-warm {
  background: var(--accent-warm);
}

.contact-btn-warm:hover {
  background: var(--accent-warm-hover);
}

@media (max-width: 768px) {
  .intro-name {
    font-size: 3rem;
  }

  .intro-tagline {
    font-size: 1.1rem;
  }

  .about-row {
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  .about-facts {
    grid-template-columns: 1fr;
  }

  .journey::before {
    left: 16px;
  }

  .journey-item:nth-child(odd) .journey-card,
  .journey-item:nth-child(even) .journey-card {
    margin-left: 40px;
    margin-right: 0;
  }

  .journey-item::after {
    left: 16px;
  }

  .contact-card {
    padding: 2.2rem 1.5rem;
  }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  if (window.AOS) {
    AOS.init({
      duration: 700,
      easing: 'ease-out-cubic',
      offset: 80,
      once: false
    });
  }
});
</script>
