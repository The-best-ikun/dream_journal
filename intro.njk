---
layout: base.njk
title: 自我介绍
description: 滚动浏览我的自我介绍 — 关于我、在学的东西，还有我走过的路
keywords: 自我介绍, 个人介绍, 研究生, 学习
useAos: true
---

<section class="intro-hero">
  <div class="intro-hero-inner" data-aos="fade-up">
    <p class="intro-kicker">HELLO，我是</p>
    <h1 class="intro-name handwriting">{{ site.author }}</h1>
    <p class="intro-roles">一名研究生 <span class="dot">·</span> 在学代码 <span class="dot">·</span> 也在学着生活</p>
  </div>
  <div class="scroll-hint" data-aos="fade-up" data-aos-delay="500">
    <span class="scroll-hint-text">向下滚动，认识我</span>
    <div class="scroll-mouse"><div class="scroll-wheel"></div></div>
  </div>
</section>

<section class="intro-section">
  <div class="section-head" data-aos="fade-up">
    <span class="section-no">01</span>
    <h2 class="section-title">关于我</h2>
    <p class="section-sub">ABOUT ME</p>
  </div>

  <div class="about-row">
    <div class="about-avatar-wrap" data-aos="fade-right">
      <div class="about-avatar handwriting">白日<br>梦游</div>
      <div class="about-avatar-ring"></div>
    </div>
    <div class="about-text" data-aos="fade-left" data-aos-delay="150">
      <h3>你好，我是 {{ site.author }}，一名还在路上的研究生。</h3>
      <p>写代码的时间不算长，水平有限，正在一点一点学。这个网站是我自己搭着玩的，边学边记，也算是个长期的练手作业。</p>
      <p>「{{ site.signature }}」这个名字，寄托了我对生活的一点小期待 —— 把白日梦慢慢走成现实。白天认真上课，晚上在文字里待一会儿，就是我最舒服的状态。</p>
      <ul class="about-facts">
        <li><strong>坐标</strong><span>中国 · 一个慢生活的小城</span></li>
        <li><strong>方向</strong><span>前端 / 全栈，还在摸索</span></li>
        <li><strong>信条</strong><span>慢慢来，比较快</span></li>
      </ul>
    </div>
  </div>
</section>

<section class="intro-section">
  <div class="section-head" data-aos="fade-up">
    <span class="section-no">02</span>
    <h2 class="section-title">我的技能</h2>
    <p class="section-sub">仅供参考</p>
  </div>

  <p class="skills-note" data-aos="fade-up">纯属个人自评，水平有限，不敢打分了：</p>
  <div class="skill-tier" data-aos="fade-up">
    <h3>日常在用的</h3>
    <div class="skill-chips">
      <span class="skill-chip">JavaScript</span>
      <span class="skill-chip">CSS</span>
      <span class="skill-chip">Vue</span>
    </div>
  </div>
  <div class="skill-tier" data-aos="fade-up" data-aos-delay="80">
    <h3>用过、但会忘的</h3>
    <div class="skill-chips">
      <span class="skill-chip">React</span>
      <span class="skill-chip">Node.js</span>
      <span class="skill-chip">TypeScript</span>
    </div>
  </div>
  <div class="skill-tier" data-aos="fade-up" data-aos-delay="160">
    <h3>只是听说过的</h3>
    <div class="skill-chips">
      <span class="skill-chip">Python</span>
      <span class="skill-chip">Docker</span>
    </div>
  </div>
</section>

<section class="intro-section">
  <div class="section-head" data-aos="fade-up">
    <span class="section-no">03</span>
    <h2 class="section-title">走过的路</h2>
    <p class="section-sub">MY JOURNEY</p>
  </div>

  <div class="journey">
    <div class="journey-item" data-aos="fade-right">
      <div class="journey-date">2024 — 至今</div>
      <div class="journey-card">
        <h4>研究生在读</h4>
        <p>跟着导师做研究，平时写点小工具、小脚本，把课上学的东西用到实践里。</p>
      </div>
    </div>
    <div class="journey-item" data-aos="fade-left">
      <div class="journey-date">2020 — 2024</div>
      <div class="journey-card">
        <h4>本科阶段</h4>
        <p>接触到编程，做课程设计和一些入门小项目，学着把想法变成代码。</p>
      </div>
    </div>
    <div class="journey-item" data-aos="fade-right">
      <div class="journey-date">再早一些</div>
      <div class="journey-card">
        <h4>第一次接触代码</h4>
        <p>写下人生第一行 Hello World，从此开始折腾，一直折腾到现在。</p>
      </div>
    </div>
  </div>
</section>

<section class="intro-section">
  <div class="section-head" data-aos="fade-up">
    <span class="section-no">04</span>
    <h2 class="section-title">我的一点坚持</h2>
    <p class="section-sub">LITTLE HABITS</p>
  </div>

  <div class="values-grid">
    <div class="value-card" data-aos="flip-up" data-aos-delay="0">
      <span class="value-num">01</span>
      <h4>慢慢来</h4>
      <p>基础还没打牢，先不急着标榜自己。</p>
    </div>
    <div class="value-card" data-aos="flip-up" data-aos-delay="120">
      <span class="value-num">02</span>
      <h4>多记录</h4>
      <p>学过的、踩过的坑都记下来，方便以后回顾。</p>
    </div>
    <div class="value-card" data-aos="flip-up" data-aos-delay="240">
      <span class="value-num">03</span>
      <h4>多请教</h4>
      <p>不懂就问，承认自己不会，比不懂装懂省心。</p>
    </div>
    <div class="value-card" data-aos="flip-up" data-aos-delay="360">
      <span class="value-num">04</span>
      <h4>多动手</h4>
      <p>光看教程不长进，自己做出东西才算数。</p>
    </div>
    <div class="value-card" data-aos="flip-up" data-aos-delay="480">
      <span class="value-num">05</span>
      <h4>少熬夜</h4>
      <p>学业重要，身体更重要，要可持续地折腾。</p>
    </div>
    <div class="value-card" data-aos="flip-up" data-aos-delay="600">
      <span class="value-num">06</span>
      <h4>按时吃饭</h4>
      <p>写代码也不能饿着肚子，吃饭是头等大事。</p>
    </div>
  </div>
</section>

<section class="intro-section intro-contact">
  <div class="contact-card" data-aos="zoom-in-up">
    <h2 class="handwriting">很高兴认识你</h2>
    <p>如果你想聊聊学习、代码，或者一起做点什么有意思的事，欢迎随时找到我。</p>
    <div class="contact-links">
      <a href="{{ site.github }}" class="contact-btn" target="_blank" rel="noopener noreferrer">GitHub</a>
      <a href="{{ site.twitter }}" class="contact-btn contact-btn-warm" target="_blank" rel="noopener noreferrer">Twitter</a>
    </div>
  </div>
</section>

<style>
/* 防止横向溢出（侧向动画会推宽页面） */
html, body { overflow-x: hidden; }

.intro-hero {
  min-height: calc(100vh - 88px);
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
  position: relative;
  padding: 4rem 0 2rem;
}

.intro-hero::before,
.intro-hero::after {
  content: '';
  position: absolute;
  border-radius: 50%;
  filter: blur(60px);
  z-index: 0;
  pointer-events: none;
}

.intro-hero::before {
  width: 360px;
  height: 360px;
  background: rgba(92, 141, 137, 0.16);
  top: 8%;
  left: 6%;
}

.intro-hero::after {
  width: 300px;
  height: 300px;
  background: rgba(200, 130, 110, 0.16);
  bottom: 10%;
  right: 8%;
}

.intro-hero-inner {
  position: relative;
  z-index: 1;
}

.intro-kicker {
  letter-spacing: 0.35em;
  font-size: 0.8rem;
  color: var(--accent-warm);
  margin-bottom: 1rem;
}

.intro-name {
  font-size: 4.5rem;
  color: var(--text-primary);
  margin-bottom: 1.2rem;
}

.intro-roles {
  font-size: 0.95rem;
  color: var(--text-muted);
  letter-spacing: 0.08em;
}

.intro-roles .dot {
  margin: 0 0.5rem;
}

.scroll-hint {
  position: absolute;
  bottom: 1.5rem;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.6rem;
  z-index: 1;
}

.scroll-hint-text {
  font-size: 0.78rem;
  color: var(--text-muted);
  letter-spacing: 0.2em;
}

.scroll-mouse {
  width: 22px;
  height: 36px;
  border: 2px solid var(--text-muted);
  border-radius: 12px;
  display: flex;
  justify-content: center;
  padding-top: 6px;
}

.scroll-wheel {
  width: 3px;
  height: 7px;
  border-radius: 2px;
  background: var(--accent);
  animation: wheelDrop 1.6s ease-in-out infinite;
}

@keyframes wheelDrop {
  0% { opacity: 1; transform: translateY(0); }
  70% { opacity: 0; transform: translateY(10px); }
  100% { opacity: 0; transform: translateY(10px); }
}

.intro-section {
  padding: 4.5rem 0;
  border-top: 1px solid var(--border-color);
}

.section-head {
  text-align: center;
  margin-bottom: 3rem;
}

.section-no {
  display: inline-block;
  font-size: 0.78rem;
  letter-spacing: 0.25em;
  color: var(--accent-warm);
  border: 1px solid var(--border-color);
  border-radius: 999px;
  padding: 0.2rem 0.9rem;
  margin-bottom: 0.9rem;
  background: var(--bg-card);
}

.section-title {
  font-size: 2.2rem;
  margin-bottom: 0.2rem;
}

.section-sub {
  font-size: 0.82rem;
  letter-spacing: 0.3em;
  color: var(--text-muted);
}

/* 关于我 */
.about-row {
  display: grid;
  grid-template-columns: 280px 1fr;
  gap: 3rem;
  align-items: center;
  max-width: 900px;
  margin: 0 auto;
}

.about-avatar-wrap {
  position: relative;
  display: flex;
  justify-content: center;
}

.about-avatar {
  width: 220px;
  height: 220px;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--accent), var(--accent-warm));
  color: #fff;
  font-size: 2rem;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  line-height: 1.4;
  position: relative;
  z-index: 1;
  box-shadow: var(--shadow-lg);
}

.about-avatar-ring {
  position: absolute;
  inset: -14px;
  border-radius: 50%;
  border: 1px dashed var(--accent-light);
  animation: spin 30s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.about-text h3 {
  font-size: 1.6rem;
  margin-bottom: 1rem;
}

.about-text p {
  color: var(--text-secondary);
  line-height: 1.9;
  margin-bottom: 0.9rem;
}

.about-facts {
  list-style: none;
  padding: 0;
  margin-top: 1.4rem;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0.5rem 1.5rem;
}

.about-facts li {
  display: flex;
  gap: 0.5rem;
  font-size: 0.92rem;
  color: var(--text-secondary);
  border-bottom: 1px solid var(--divider);
  padding-bottom: 0.5rem;
}

.about-facts strong {
  color: var(--accent);
  font-weight: 600;
  min-width: 2.2em;
}

/* 技能 */
.skills-note {
  text-align: center;
  font-size: 0.9rem;
  color: var(--text-muted);
  margin-bottom: 1.6rem;
}

.skill-tier {
  max-width: 560px;
  margin: 0 auto 1.5rem;
  text-align: center;
}

.skill-tier h3 {
  font-size: 1rem;
  color: var(--accent);
  margin-bottom: 0.6rem;
}

.skill-chips {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 0.5rem;
}

.skill-chip {
  display: inline-block;
  padding: 0.3rem 0.9rem;
  font-size: 0.85rem;
  border-radius: 999px;
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  color: var(--text-secondary);
}

/* 走过的路 */
.journey {
  position: relative;
  max-width: 820px;
  margin: 0 auto;
  padding: 1rem 0;
}

.journey::before {
  content: '';
  position: absolute;
  left: 50%;
  top: 0;
  bottom: 0;
  width: 1px;
  background: var(--border-color);
  transform: translateX(-50%);
}

.journey-item {
  position: relative;
  margin-bottom: 2.5rem;
}

.journey-item:nth-child(odd) .journey-card {
  margin-right: 52%;
}

.journey-item:nth-child(even) .journey-card {
  margin-left: 52%;
}

.journey-item::after {
  content: '';
  position: absolute;
  left: 50%;
  top: 12px;
  width: 11px;
  height: 11px;
  border-radius: 50%;
  background: var(--accent);
  border: 2px solid var(--bg-primary);
  transform: translateX(-50%);
  z-index: 1;
}

.journey-card {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 8px;
  padding: 1.1rem 1.3rem;
}

.journey-date {
  font-size: 0.8rem;
  color: var(--accent-warm);
  font-weight: 600;
  margin-bottom: 0.35rem;
  letter-spacing: 0.04em;
}

.journey-card h4 {
  font-size: 1.05rem;
  margin-bottom: 0.3rem;
}

.journey-card p {
  font-size: 0.88rem;
  color: var(--text-secondary);
  line-height: 1.7;
}

/* 坚持 */
.values-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 1.25rem;
  max-width: 900px;
  margin: 0 auto;
}

.value-card {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 10px;
  padding: 1.6rem 1.4rem;
  text-align: center;
  transition: box-shadow 0.25s ease, transform 0.2s ease;
}

.value-card:hover {
  box-shadow: var(--shadow-md);
  transform: translateY(-3px);
}

.value-num {
  font-family: 'Kalam', cursive;
  font-size: 1.6rem;
  color: var(--accent-light);
  display: block;
  margin-bottom: 0.5rem;
}

.value-card h4 {
  font-size: 1.05rem;
  margin-bottom: 0.5rem;
  color: var(--accent);
}

.value-card p {
  font-size: 0.85rem;
  color: var(--text-secondary);
  line-height: 1.6;
}

/* 联系 */
.intro-contact {
  border-bottom: 1px solid var(--border-color);
}

.contact-card {
  max-width: 640px;
  margin: 0 auto;
  text-align: center;
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 14px;
  padding: 3rem 2.5rem;
  background-image: radial-gradient(circle at 15% 20%, rgba(92,141,137,0.1), transparent 60%),
    radial-gradient(circle at 85% 80%, rgba(200,130,110,0.12), transparent 60%);
}

.contact-card h2 {
  font-size: 2.2rem;
  margin-bottom: 0.8rem;
}

.contact-card p {
  color: var(--text-secondary);
  margin-bottom: 2rem;
}

.contact-links {
  display: flex;
  gap: 1rem;
  justify-content: center;
}

.contact-btn {
  display: inline-block;
  padding: 0.7rem 1.8rem;
  border-radius: 999px;
  text-decoration: none;
  font-size: 0.92rem;
  font-weight: 500;
  color: #fff;
  background: var(--accent);
  transition: background 0.2s ease, transform 0.2s ease;
}

.contact-btn:hover {
  background: var(--accent-hover);
  transform: translateY(-2px);
}

.contact-btn-warm {
  background: var(--accent-warm);
}

.contact-btn-warm:hover {
  background: var(--accent-warm-hover);
}

@media (max-width: 768px) {
  .intro-name {
    font-size: 3rem;
  }

  .intro-tagline {
    font-size: 1.1rem;
  }

  .about-row {
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  .about-facts {
    grid-template-columns: 1fr;
  }

  .journey::before {
    left: 16px;
  }

  .journey-item:nth-child(odd) .journey-card,
  .journey-item:nth-child(even) .journey-card {
    margin-left: 40px;
    margin-right: 0;
  }

  .journey-item::after {
    left: 16px;
  }

  .contact-card {
    padding: 2.2rem 1.5rem;
  }
}
</style>
